# net-limiter
A lightweight Windows application that can **limit or block internet access per process** using PowerShell.   Built with **Go** and **Fyne**, this tool provides a simple GUI for controlling network bandwidth or fully blocking connections for any running executable.
